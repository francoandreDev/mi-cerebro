import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.frnco.micerebro',
  appName: 'mi-cerebro',
  webDir: 'dist/mi-cerebro/browser',
};

export default config;
