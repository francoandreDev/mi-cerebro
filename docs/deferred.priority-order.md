# Diferidos — orden de prioridad por impacto/esfuerzo

Ranking de `docs/deferred/index.md` (ítems no resueltos). A diferencia de la versión anterior de este documento (que ordenaba solo por esfuerzo), esta versión cruza dos ejes:

- **Impacto** — Alto / Medio / Bajo. Alto = rompe algo que ya debería funcionar (bug), o bloquea un flujo prometido explícitamente en otro doc (home guide, redesigns), o toca una sección de uso diario (notas, tasks, reminders, calendario, búsqueda, sync). Medio = mejora real en una sección de uso frecuente sin bloquear nada, o cierra un gap de UX notorio. Bajo = polish visual, casos raros/baja frecuencia, o ítems que el propio `deferred/index.md` marca como YAGNI/optimización prematura.
- **Esfuerzo** — Tier 1 (trivial) a Tier 5 (nueva arquitectura), mismo criterio que la versión anterior: superficie tocada, dependencia de decisión UX, nuevas dependencias/migraciones.

Orden final dentro de cada nivel de impacto: esfuerzo ascendente. No reemplaza a `deferred/index.md` — es una lente de secuenciación; cuando se aborde un ítem, sigue cerrándose ahí.

**Los ratings de impacto son un juicio editorial, no una medición real de uso.** Si algo se siente mal priorizado en la práctica, decilo y se recalibra — no hay telemetría detrás de esto, es lectura de los propios docs (qué se prometió, qué está roto, qué se usa a diario según el roadmap).

---

## Impacto Alto

**El grupo Impacto Alto no tiene ítems tomables ni bloqueados** — todo lo que había se cerró. Próximo paso: bajar a Impacto Medio o Bajo.

Cerrado: ~~[BLOQUEADO — requiere decisión de diseño previa] Árbol de carpetas huérfano tras los rediseños de tarjetas~~ (esfuerzo: terminó siendo Tier 3, breadcrumb + drill-down) — ya estaba resuelto (documentación de cierre 2026-08-04): esta entrada seguía citando `deferred/index.md`, que ya no tiene esa entrada — se resolvió en `docs/proyecto/roadmap-22-25.md` §23 (decisión de diseño: breadcrumb + drill-down tipo explorador de archivos por sección, contrato `?folder=<path>`, componente compartido `shared/folder-breadcrumb/`), documentado en `docs/sistema/dashboard-carpetas-metas-dormidas.md`. Las 8 secciones de tarjetas migraron completas.

Cerrado: ~~Auditar y arreglar las reglas `:global(...)` de `editor.component.css`~~ (esfuerzo: medio) — resuelto 2026-07-08, las 9 reglas afectadas se movieron a `src/styles/_editor-content.scss`.
Cerrado: ~~Quick-capture global de nota desde cualquier sección~~ (esfuerzo: alto, Tier 4) — resuelto 2026-07-09, atajo `Alt+Shift+N` + overlay global que crea la nota sin salir de la sección actual, ver `deferred/index.md` para el detalle completo.
Cerrado: ~~Vista unificada cross-section por tag~~ (esfuerzo: alto, Tier 4) — resuelto 2026-07-09, `TaggedItemsService` + ruta `/tags/:id` + acceso desde el palette con `tag:nombre`, ver `deferred/index.md` para el detalle completo (incluye el descubrimiento de arquitectura sobre `shared/entity-cards/`).

## Impacto Medio

Cerrado: ~~Snooze próximo lunes / fin de semana + menú overflow `⋯` (reminders)~~ (esfuerzo: trivial, Tier 1) — resuelto 2026-07-06 en `1508ac9` (ver `reminders-goals.md`); no detectado hasta ahora porque este ranking no se había regenerado desde entonces.

Cerrado: ~~Toggle "ver sólo cambios" en diffs largos (`/history`)~~ (esfuerzo: trivial, Tier 1) — resuelto 2026-07-16 (ver `versionado.md`).

Cerrado: ~~Snooze inteligente del goal-reminder~~ (esfuerzo: fácil, Tier 2) — resuelto 2026-07-16, snooze skip-one vía `RemindersCadenceService.skipNextSlot()` (ver `reminders-goals.md`).

Cerrado: ~~Navegación por carpetas / DnD entre carpetas en `/files`~~ (esfuerzo: fácil, Tier 2) — resuelto en §19.23b (breadcrumb + drill-down `?folder=<path>`, `shared/folder-breadcrumb/`), no detectado hasta ahora porque este ranking no se había regenerado desde entonces (ver `deferred/files-writings-tasks.md`).

Cerrado: ~~Sobreescritura completa de defaults del navegador en combos consumidos~~ (esfuerzo: fácil, Tier 2) — resuelto 2026-07-17, re-audit completo encontró y corrigió 2 bugs reales (falta de `preventDefault()` en `tts-controls`, colisión `F11`↔fullscreen nativo cambiada a `Alt+Shift+F`); ver `shortcuts-cross-section.md`.

Cerrado: ~~Migrar `findPath()`/`findChapterFile` a `MCB-FS-008`~~ (esfuerzo: fácil, Tier 2) — resuelto 2026-07-16 (ver `editor-history-fs.md` y `docs/errors.md`).

Cerrado: ~~Vista de papelera~~ (esfuerzo: medio, Tier 3) — ya estaba resuelto (`/trash`, `src/app/features/trash/`), no detectado hasta ahora porque este ranking no se había regenerado desde que se implementó; entrada correspondiente en `notes-search-navigation.md` corregida.

Cerrado: ~~Recordatorios automáticos para tareas / escritos con deadline~~ (esfuerzo: medio, Tier 3) — resuelto 2026-07-16, `TaskRemindersSyncService`/`WritingRemindersSyncService` extienden el patrón goal-sourced (ver `reminders-goals.md` y `features.md` §14).

Cerrado: ~~Menú ⋯ con duplicar / exportar a Markdown (books)~~ (esfuerzo: medio, Tier 3) — resuelto 2026-07-17, `BooksService.duplicateBook`/`readAllChaptersOrdered` + converter `tiptapToMarkdown` en `@core/tiptap/markdown/` (ver `trash-books.md`, entrada eliminada).

Cerrado: ~~Cover art y duración ID3 (`jsmediatags`)~~ (esfuerzo: alto, Tier 4) — resuelto (documentación de cierre 2026-07-18), extracción y persistencia ya existían de un paso anterior; sólo faltaba la miniatura real en el header de álbum de la biblioteca (ver `deferred/music.md`).

Cerrado: ~~Reschedule de tareas con DnD en el calendario~~ (esfuerzo: medio, Tier 3) — resuelto 2026-07-20, arrastrar una tarea desde la tarjeta de kind lateral (`mc-calendar-kind-card`) a un día de la grilla mensual (`mc-calendar-light-table`) reagenda esa fecha específica vía `TasksService.rescheduleDueDate` (nuevo) + `CalendarEventsService.rescheduleTask`, reusando el mime custom `MC_INTERNAL_DND_TYPE` del patrón de DnD compartido (ver `shortcuts-cross-section.md`, entrada eliminada).

    Cerrado: ~~IDs de libros legibles / acortados en la URL~~ (esfuerzo: alto, Tier 4) — ya estaba resuelto (documentación de cierre 2026-08-04): §19.20c cerró `entitySlugSegment`/`extractEntityId` (`core/routing/entity-slug.ts`) hace tiempo, cross-feature (26 archivos, las 8 entidades incluidas books/capítulos) — URL formato `<slug-del-título>-<uuid>`, cosmético (no reemplaza el uuid como fuente de verdad, así que renombrar no rompe links guardados). Esta entrada de `priority-order.md` había quedado desactualizada desde entonces; no había entrada correspondiente en `deferred/index.md` para borrar.

Cerrado: ~~Referencias / links entre entidades desde el editor~~ (esfuerzo: alto, Tier 4) — ya estaba resuelto en su mayor parte (documentación de cierre 2026-08-04): §27 ("Conexiones entre entidades", ver `docs/sistema/conexiones.md`) ya había cerrado el modelo (`RelationsService`, `relations.json`), el nodo `entityRef`, el picker (`EntityLinkPickerDialogComponent` sobre `SearchIndexService`), resolución de título en vivo ante rename/borrado (`resolveRelations`), panel "Conexiones" y grafo de 1 salto — el único hueco real contra esta entrada era la sintaxis "desde el editor" tipeando, no sólo por botón. Se agregó el trigger `@` (`core/tiptap/entity-mention/entity-mention.ext.ts`, `InputRule` sobre el mismo picker/flujo de `triggerLink()`, sin necesidad de `@tiptap/suggestion` porque el picker es un diálogo centrado que roba foco al abrir, no una lista flotante inline) + `mcAutofocus` en el input de búsqueda del picker (antes sin autofocus, ahora necesario para que el flujo de tipeo no se corte). Ver `docs/sistema/conexiones.md`.

Cerrado: ~~Atajos de navegación de fila (J/K, Space, E, Del) en tasks y goals~~ (esfuerzo: alto, Tier 4) — ya estaba resuelto (documentación de cierre 2026-08-05): la entrada decía que tasks (kanban de buckets) y goals (wall de constelación) no tenían "fila" a la que mapear J/K por no ser listas lineales, pero ambas secciones ya tienen un modo "Ver como lista" (toggle en el header, `viewMode() === 'list'`) que expone exactamente esa vista lineal para navegación por teclado, mientras el modo visual (jardín/muro) queda intacto para el resto de los casos. `tasks-garden.container.ts` y `goals-wall.container.ts` ya cablean el primitivo compartido `RowNavController` (`shared/utils/row-nav.controller.ts`, extraído del inline original de reminders) con `rowIds` ordenados por `position`/comparator de cada servicio — mismo patrón J/K/E/Space/Supr que reminders. No había entrada correspondiente en `deferred/reminders-goals.md` para borrar (el ítem nunca se había registrado ahí, sólo vivía en este ranking).

15. **CORS proxy propio para push/fetch a GitHub** (esfuerzo: alto, Tier 4 — infraestructura externa, Cloudflare Worker) — el bug de `pushAll` (cerrado 2026-07-08) resultó no tener nada que ver con el proxy — era client-side puro. El proxy público sigue siendo un single point of failure operado por terceros, pero deja de ser sospechoso de nada concreto.
    **Restricción de secuenciación (2026-08-05, a pedido explícito del usuario):** este ítem se aborda **último de toda la lista**, después de agotar Impacto Bajo — no por su Tier (4, igual que varios de Bajo), sino porque requiere levantar infraestructura externa al repo (elegir hosting, configurar el Worker, exponerlo en `/settings`) en vez de ser trabajo puramente interno. Sigue siendo Impacto Medio en términos de valor; la restricción es sólo de orden de toma.

Cerrado: ~~Responsive mobile — resto de pantallas sin `@media`~~ (esfuerzo: medio, Tier 3) — ya estaba resuelto (documentación de cierre 2026-08-04): §19.21d cerró las 12 pantallas restantes (tasks/goals/tags/files/variants/trash/images/bookshelf-reader + la pasada de huecos en calendar/history/music/settings/sync/writings-shelf/reminders), ver `docs/sistema/navegacion-descargas-responsive.md`. Esta entrada había quedado desactualizada. Lo único genuinamente pendiente es la **verificación visual en navegador real** de esas 12 pantallas — bloqueada por el bridge de screenshot de Chrome, no por trabajo mecánico faltante — y el rediseño mobile dedicado de los 3 modos de zoom de History (decisión de UX, no mecánico). Ambos quedan en `docs/deferred/responsive.md`; el usuario dijo que haría su propia pasada de revisión visual.

## Impacto Bajo

Polish visual, casos de baja frecuencia, o ítems que el propio `deferred/index.md` marca como YAGNI/optimización prematura.

**Orden de toma (2026-08-05, a pedido explícito del usuario):** reordenado por Tier ascendente y cercanía (ítems de la misma sección agrupados dentro del mismo Tier), en vez del orden histórico de descubrimiento. Los ítems que antes decían "no ahora"/"decisión de producto ya tomada" se reactivan como tomables en su Tier correspondiente — la razón original de cada uno sigue documentada abajo como contexto de diseño a respetar al implementarlos, no como veto. El ítem #15 de Impacto Medio (CORS proxy) va **después de todo este grupo**, ver esa entrada para la restricción completa.

Cerrado: ~~Filtros por tipo de entidad~~ (Tier 2) — ya estaba resuelto (documentación de cierre 2026-08-04): el bloqueo que citaba esta entrada ("faltan otras entidades indexadas") dejó de aplicar hace rato — `SearchIndexService` ya indexa 11 kinds y el filtro `kind:<tipo>` del palette (`parsePaletteQuery`, `command-palette.container.ts`) ya funciona, documentado en `docs/sistema/conexiones.md` ("Filtro por tipo en la búsqueda global").

Cerrado: ~~Lead-time por meta~~ (Tier 1) — ya estaba resuelto (documentación de cierre 2026-08-04): `Goal.reminderLeadMinutes` (schema v9→v10, `goal.types.ts`, `reminder-lead-deadline-time.migration.ts`) es un override per-goal, no un único valor global. El YAGNI original ("mientras un único lead-time alcance") ya no describe el estado real.

Cerrado: ~~Hora del deadline configurable~~ (Tier 2) — ya estaba resuelto (documentación de cierre 2026-08-04): `Goal.deadlineTime` (mismo schema v10, `DEFAULT_DEADLINE_TIME = '23:59'` sólo como fallback) con UI en `goal-constellation-editor.component.ts`, usado por `goalDeadlineInstant()`/`goal-reminders-sync.service.ts`. Ya no está hardcodeado.

Cerrado: ~~"Cargar más" en backlog / tasks~~ (Tier 2) — ya estaba resuelto (documentación de cierre 2026-08-04): `tasks-garden.container.ts` (`visibleBacklogCount`/`hiddenBacklogCount`/`loadMoreLabel`) ya pagina el backlog en vez de cargar todo de una.

Cerrado: ~~Pin/fijado de estantes (books)~~ (Tier 3) — ya estaba resuelto (documentación de cierre 2026-08-04): `bookshelf-prefs.ts` (`PINNED_FOLDERS_KEY`) + `bookshelf.container.ts` (`togglePin`, `pinnedFolders`) + `sortNamedFoldersPinnedFirst`. El YAGNI "hasta N>10 estantes" quedó obsoleto.

Cerrado: ~~Drag-to-reposition de estrellas (goals)~~ (Tier 3) — ya estaba resuelto (documentación de cierre 2026-08-04): esto es el editor `/goals/:id` reposicionando pasos, no el wall que lista metas (ver ítem siguiente, es otra feature) — `goal-constellation-editor.component.ts` (`onStarDown`/`onStarMove`/`onStarUp`) ya permite arrastrar un paso existente y persiste el nuevo `x/y` vía `stepsChange.emit(...)`, con el mismo threshold de 4px para distinguir click de drag que describía el ítem original en `deferred/reminders-goals.md`. "La creación con click ya cubre el caso principal" ya no es la única vía.

Cerrado: ~~Layout libre de la constelación (goals wall)~~ (Tier 3) — ya estaba resuelto (documentación de cierre 2026-08-04, feature distinta de la anterior pese al nombre parecido — esa era el editor de una meta, ésta es el wall que lista todas): `Goal.wallCenter` (`goal.types.ts`) persiste una posición libre real por meta; `centerOf()` (`goal-wall-layout.utils.ts`) sólo cae al centroide hash-based cuando `wallCenter` está ausente. "El hash-based ya cubre sin nuevo estado" es falso — sí hay estado nuevo y ya se usa.

Cerrado: ~~Tooltip por-día en la panorámica (`/history`)~~ (Tier 3) — ya estaba resuelto (documentación de cierre 2026-08-04): `history-panorama.component.html` ya renderiza `.panorama-tooltip` (`tooltipFacets`/`columnTitle`) al hacer hover sobre una columna del día.

Cerrado: ~~Typewriter focus línea-por-línea (editor)~~ (Tier 3) — ya estaba resuelto (documentación de cierre 2026-08-04): `core/tiptap/typewriter-focus/typewriter-focus.ext.ts` ya es un plugin ProseMirror por decoración que difumina (`.mc-typewriter-dim`) cada bloque salvo el que tiene el cursor (`blockIdAt`) — foco real por bloque/línea, no la "máscara CSS de rango amplio" que describía el ítem original.

Cerrado: ~~Typewriter mode en `/writings`~~ (Tier 3) — ya estaba resuelto (documentación de cierre 2026-08-04): `writing-editor-pane.component.ts` ya inyecta `TypewriterModeService` y aplica `[class.typewriter-mode]` en el host, mismo toggle que el resto del editor.

Cerrado: ~~Animaciones orgánicas de DnD (tasks)~~ (Tier 3) — ya estaba resuelto (documentación de cierre 2026-08-04): `plant-card.component.css` ya tiene "DnD orgánico" real — raíces colgando y balanceándose mientras se arrastra (`.card--lifted::after`, `roots-sway`) y "plop" elástico al soltar en un cantero nuevo (`.card--plop`, `plant-plop`), ambos con comentario explícito citando "DnD orgánico" en el CSS.

Cerrado: ~~Riego con cursor regadera (tasks)~~ (Tier 3) — ya estaba resuelto (documentación de cierre 2026-08-04): `tasks-garden.container.ts` (`WATERING_KEY`, signal `watering`) + `tasks-garden.container.css` (`.garden--watering { cursor: url(...) }` con hotspot real) ya cablean el cursor de regadera.

Cerrado: ~~Cesta de cosecha con salto en arco (tasks)~~ (Tier 3) — ya estaba resuelto (documentación de cierre 2026-08-04): `TasksGardenContainer.flyToBasket()` clona la card, la anima con `plant-arc` (`plant-card.component.css`, `cubic-bezier` + keyframe de escala/rotación) desde la posición original hasta el centro de `.basket-stack .basket` — es la animación de salto en arco que pedía el ítem, no una animación FLIP genérica pero cumple el mismo objetivo visual.

Cerrado: ~~Export PNG/SVG de las capas (listas)~~ — resuelto 2026-07-20, botón "Exportar" (`mc-menu-button`) en la toolbar de tiza serializa las capas visibles a un SVG standalone (puntos normalizados horneados a px reales) y opcionalmente rasteriza a PNG vía canvas (`svgToPngBlob`), descarga con `triggerDownload` (ver `deferred/lists-images.md`, entrada eliminada).

Cerrado: ~~Undo/redo dedicado para trazos (listas)~~ (Tier 3) — ya estaba resuelto (documentación de cierre 2026-08-04): `chalk-history.utils.ts` (`undoChalkHistory`/`redoChalkHistory`, historial local session-scoped cap 50) ya existe, deliberadamente separado del Ctrl+Z de TipTap.

Cerrado: ~~Estilo "pizarra de verdad" en todo el pane~~ — resuelto 2026-08-03, toggle "vista pizarra completa" (off por default) en `/lists/:id`, restyle scopeado a esa instancia del editor compartido vía un input nuevo + host class, sin tocar `--mc-*` ni romper el modo lectura (ver `deferred/lists-images.md`, entrada eliminada).

Cerrado: ~~Drag-and-drop de tracks a playlist (música)~~ — resuelto 2026-07-18, auto-switch de tab + drop en fila de playlist (ver `deferred/music.md`).

Cerrado: ~~Thumbs reales 2×2 para galerías en la papelera~~ — ya estaba resuelto (documentación de cierre 2026-07-21): `trash-card-content.component.html` ya renderiza `<img [src]="url">` reales por tile (`galleryTiles()`), con `TrashService.loadGalleryCovers` → `readTrashCoverBlobs` leyendo bytes reales de disco; sólo el ícono genérico 📷 aparece en tiles vacíos (menos de 4 imágenes en la galería borrada), no como placeholder universal. Esta entrada de `deferred.priority-order.md` había quedado desactualizada.

Cerrado: ~~Volumen real (`BookVolumeComponent`) en la papelera~~ — resuelto 2026-07-20, la card de libro en `/trash` reusa `mc-book-volume` (`shared/entity-cards/`) con `accent`/cantidad de capítulos leídos del `BookBundle` guardado, en vez del placeholder tipográfico (ver `deferred/trash-books.md`, entrada eliminada).

Cerrado: ~~Override de imágenes para portada/miniaturas (books)~~ (Tier 4) — ya estaba resuelto (documentación de cierre 2026-08-04): `BookImagesService.setFace()`/`clearFace()` ya sube un blob real a `cover.<ext>`/`back.<ext>`, con UI completa (subir/previsualizar/quitar) en `book-open.container.ts` (`pickFace`/`coverFileInput`); `BookFaceRef` (`book.types.ts`) soporta `kind: 'image'` además del face procedural (`kind: 'auto'`).

Cerrado: ~~Subset + conversión a woff2 de Crimson Pro~~ — resuelto 2026-08-04, `pyftsubset` (fonttools) instalado en un venv aislado (no disponible como paquete del sistema); subset a la unión de los rangos Unicode "latin" + "latin-ext" de Google Fonts sobre los 3 `.ttf` originales, recortando cada `.woff2` de ~34-35 KB a ~28-29 KB (~17%, no el ~30% estimado — la fuente ya traía pocos glyphs fuera de ese rango). Verificado con `fontTools`: los 601 glyphs resultantes cubren todos los acentos/ñ/¿¡ del español y el resto de latin-ext (ver `deferred/trash-books.md`, entrada eliminada).

Cerrado: ~~Waveform pre-renderizado (música)~~ — ya estaba implementado (decodificado on-demand + cacheado en memoria, no persistido a disco); sólo la entrada de `deferred/` había quedado sin cerrar (documentación de cierre 2026-07-18).

Cerrado: ~~Crypto-at-rest para PAT en `secrets.json`~~ (Tier 4) — resuelto 2026-08-04: el token se persiste cifrado (AES-GCM, `pat-crypto.ts`) con una clave no-extraíble por dispositivo guardada en IndexedDB, sin passphrase — el diseño passphrase-per-boot que descartaba la nota original de deferred sigue descartado (rompía el flujo "abrir y editar"), pero una clave atada al navegador da protección real contra copia/backup de la carpeta del workspace sin costo de UX. Archivos v1 (plano) se migran solos al primer boot. Ver `docs/proyecto/features.md` "Dónde vive el PAT".

Cerrado: ~~Índice de búsqueda global para comentarios~~ (Tier 4) — resuelto 2026-08-04: `comment` es un kind nuevo en el índice global existente (`SearchIndexService`), poblado por un walk de priming (`SearchFamilyPrimingService`) de la rama `comments` de la familia activa al boot y en cada switch de variante, más reindexado en vivo en cada `CommentsService.save()` (`SearchIndexService.replaceEntity`). Ver `docs/proyecto/features.md` "Índice de búsqueda de comentarios y borradores".

Cerrado: ~~Índice de búsqueda global para borradores~~ (Tier 4) — resuelto 2026-08-04, mismo mecanismo que comentarios: kind `draft`, priming de la rama `draft`, reindexado en vivo en `DraftsService.save()`. Ver `docs/proyecto/features.md`.

Cerrado: ~~Sala 3D real (three.js) para `/images`~~ — resuelto 2026-08-03, sala navegable en primera persona (three.js + angular-three/NGT: paredes/piso/techo como Mesh, cuadros con textura real, spotlight cenital con sombras reales, drag-to-look + WASD, raycasting para abrir/borrar), scopeada estrictamente a `/images` — Books/Lists 3D sigue siendo una fase futura sin tocar. Toggle `3D / Lista` mantiene el fallback 2D como vía de teclado/screen-reader (ver `deferred/lists-images.md`, archivo eliminado por quedar vacío).

Cerrado: ~~Color picker custom para tag (Tier 2)~~ — resuelto 2026-08-05, `Tag.colorHex` nuevo (paralelo a `colorSwatchId`, mutuamente excluyentes vía `TagsService.setCustomColor`/`setSwatch`) + `resolveTagColor(theme, tag)` en `theme-palette.ts` centraliza la prioridad `colorHex > colorSwatchId (tagHexFor) > color` (hash determinístico) para los 5 consumidores reales — de paso corrigió un bug menor: `file-locker.component.ts`/`file-locker-card.component.ts` leían `tag.color` directo e ignoraban `colorSwatchId`. UI: `<input type="color">` nativo en la paleta de `tag-picker.component.ts`, con warning no-bloqueante (ratio < 3:1 contra `--mc-bg-elevated` real vía `getComputedStyle`, mismo criterio que las 12 curated `TAG_SWATCHES`) — regla §4.13.29 "advierte, no bloquea".

Cerrado: ~~Atajos directos a herramientas y colores (tiza) (Tier 2)~~ — ya estaba resuelto (documentación de cierre 2026-08-05): `chalk-shortcuts.ts` (`registerChalkShortcuts`, wireado en `chalkboard-overlay.component.ts`) ya cablea `Ctrl+Shift+T` (toggle), `B`/`E` (herramienta), `1-5` (color por índice de `CHALK_COLORS`) y `[`/`]` (undo/redo), scope `editable-safe` para no competir con TipTap ni inputs. Esta entrada de `priority-order.md` había quedado desactualizada.

Cerrado: ~~Posición libre real (drag x/y) en `/files` (Tier 3)~~ — ya estaba resuelto (documentación de cierre 2026-08-05): `files.container.ts` (`onToggleFreeLayout`/`onItemMove`) + `FilesService.setFreeLayout`/`setItemPosition` ya persisten un x/y continuo opcional por casilla, el jitter determinista sigue siendo el default. Esta entrada había quedado desactualizada.

Cerrado: ~~Hilos entre items relacionados (`/files`) (Tier 3)~~ — resuelto 2026-08-05: `files.container.ts` agrega una capa SVG (`.thread-layer`, dentro de `.wall-canvas` nuevo envolviendo `.wall-grid`) que dibuja una línea persistente entre cada par de casillas visibles con una `Relation` real entre sí (mismo mecanismo de creación por drag-and-drop que ya existía). Posiciones medidas en vivo vía `getBoundingClientRect()` sobre `viewChildren('slot')`, recalculadas con `ResizeObserver` sobre el contenedor (mismo patrón que `chapter-editor-pane.component.ts`) — no hay layout x/y guardado para el índice de colecciones, sólo dentro de una colección vía `freeLayout` (ítem separado, ya cerrado). No verificado visualmente en navegador (sin dev server corriendo en esta sesión) — sólo `tsc --noEmit` limpio.

Cerrado: ~~Lasso selection para multi-select de pasos (goals) (Tier 3)~~ — resuelto 2026-08-05: `goal-constellation-editor.component.ts` agrega `onCanvasPointerDown`/`Move`/`Up` sobre el `<svg class="canvas">` — arrastrar desde el fondo (no desde una estrella, que ya corta la propagación de su propio `pointerdown`) dibuja un rectángulo (`.lasso`, mismo `viewBox 0 0 100 100`) y selecciona en vivo los pasos dentro de esa caja vía `MultiSelectController.setSelected()` (método nuevo, bulk-replace en vez de toggle uno por uno). Shift+drag agrega al selection existente (snapshot del set al iniciar el drag) en vez de reemplazarlo, mismo modificador que shift+click. Mismo umbral de 4px→2px (escala % vs px) y mismo flag `suppressCanvasClick` que ya usaba el drag de estrellas para no disparar "crear paso nuevo" al soltar. No verificado visualmente en navegador (sin dev server corriendo en esta sesión) — sólo `tsc --noEmit` limpio.

Cerrado: ~~Granularidad por faceta en merge bundle (Tier 3)~~ — resuelto 2026-08-05: `MergeFacetOptions` nuevo (`merge.types.ts`, `{ comments, draft }`, default `{ true, true }` preserva el comportamiento anterior) se enhebra por `MergeService.apply()` → `commitOneSelection()`, condicionando los `mergeCommentsFaceta`/`mergeDraftsFaceta` que antes corrían siempre. UI: `merge.container.ts`/`.html` agrega 2 checkboxes globales ("Traer comentarios"/"Traer borrador") — global por corrida, no por fila, porque estas facetas no tienen diff propio en el plan (no hay filepath por comentario/borrador para seleccionar individualmente). Sólo aplica a merges variant→variant; un merge desde remote nunca tocó estas facetas. No hay test dedicado — `MergeService` no tenía suite propia (isomorphic-git de por medio, verificado manualmente según el comment en `merge-drafts.spec.ts`), sólo `tsc --noEmit` limpio.

Cerrado: ~~Parser de fecha natural — alcance ampliado (Tier 3)~~ — resuelto 2026-08-05: `parse-due.ts#parseQuickAdd` agrega `parseTrailingHint()` — sin `@`, prueba la cola del título (hasta 4 palabras, de la más larga a la más corta) contra `parseDueHint()` y sólo la consume si matchea una fecha/hora real ("Pagar alquiler viernes" ⇒ título "Pagar alquiler" + próximo viernes 9am); un título sin fecha reconocible, o de una sola palabra, queda intacto (no confunde un título de 1 palabra con una fecha). Con `@` explícito el comportamiento previo no cambia. 5 tests nuevos en `parse-due.spec.ts` (15 total, todos verdes vía `ng test`).

Cerrado: ~~Textura realista de tiza (Tier 3)~~ — ya estaba resuelto (documentación de cierre 2026-08-05): `chalk-board.component.html` ya tiene un filtro SVG `#chalk-grain` (`feTurbulence` fractalNoise + `feColorMatrix` + `feComposite`) aplicado a cada `<g>` de capa, compartido entre todas para mantenerlo barato en trazos largos — el propio comentario `why` del filtro cita textualmente "Textura realista de tiza" como el ítem que resuelve. Esta entrada de `priority-order.md` había quedado desactualizada (el grep que la originó buscaba mal el patrón, o corría antes de que esto se implementara).

Orden de toma, Tier ascendente:

1. Compactación manual sobre rango específico (Tier 3) — ya existe un disparador manual (`history-strata.component.ts#onCompactNow()`, botón "compactar ahora" → `compactionScheduler.runOnce({ ignoreThreshold: true })`), pero sigue operando sobre los buckets elegibles del scheduler, no sobre un rango arbitrario elegido por el usuario.
2. Header del editor "n commits desde milestone" (Tier 3) — sin referencias a `milestone` en `shared/editor/` ni en el toolbar; `milestone.service.ts` sólo se usa hoy en `features/history`. Cercano a #1 (history).
3. Animaciones de snooze / gestos manuales (palomar) (Tier 3) — las animaciones críticas del scheduler ya están, esto es polish adicional.
4. Detalles bonitos: plumitas, plumaje, ronroneo (palomar) (Tier 3) — pulido visual de baja prioridad. Cercano a #3 (palomar).
5. Palomares temáticos por categoría (Tier 3) — los filtros del MVP ya resuelven la saturación, esto sería un layer visual encima. Cercano a #3/#4 (palomar).
6. Pulido visual general de `/history` (Tier 4) — agrupador sin límite claro, entra cuando haya uso real. Cercano a #1/#2 (history).
7. Paginación real persistida fila por fila (books) (Tier 4) — reactivado 2026-08-05: la decisión de diseño explícita ("la UI no debe mentir" — no hay ancho de referencia único) sigue siendo la restricción de implementación a respetar, no un veto a que se aborde; cualquier solución debe seguir sin fingir un ancho de referencia que no existe.
8. Estantería con forma creativa (books) (Tier 4) — visión a futuro, el shelf clásico aún no está pulido. Cercano a #7 (books).
9. Índice de búsqueda persistido por familia (`idx-main`) (Tier 4) — reactivado 2026-08-05, con salvedad: sigue siendo arquitectónicamente de bajo valor mientras `refreshAll()` recorra disco en cada switch de variante (ver `deferred/versionado.md`) — si se toma, empezar por confirmar si esa precondición cambió, o el trabajo no rinde.
10. Variantes sobre el fallback sin isomorphic-git (Tier 5) — contingencia, sólo aplica si el fallback de snapshots se activa en 13a; no es un ítem tomable por decisión propia salvo que eso ocurra.
11. `.git/` en OPFS (Tier 5) — aceptado explícitamente vivir con loading screens hasta que sea intolerable; contingencia, no roadmap.

---

## Notas de uso

- Los ratings de impacto son juicio editorial basado en lo que los propios docs (`deferred/index.md`, redesigns, home guide) prometen o marcan como uso diario — no hay telemetría real detrás. Si algo no calza con el uso real, decilo y se recalibra.
- Dentro de "Impacto Bajo" no vale la pena pelear el orden exacto — la diferencia entre ítem #30 y #40 es marginal. El valor de la lista está en separar Alto/Medio del resto, no en el ranking fino de la cola.
- Varios ítems de esfuerzo alto comparten infraestructura (los tres índices de búsqueda persistidos, por ejemplo) — conviene abordarlos juntos aunque compartan nivel de impacto.
- Este documento es una foto (regenerado 2026-07-08); si `deferred/index.md` cierra o agrega ítems, este ranking debería regenerarse — no editarse a mano ítem por ítem.
- Historial de cierres previos (pre-2026-07-08): ver `deferred/index.md` para el detalle — incluye reindexar manual, snippet con highlight, UI de tags, badge de vencidas, umbral de compactación configurable, dev panel de compactación, error code `MCB-MUS-001`, colapsar chips de kind, continuidad de última ruta, historial de búsquedas recientes, anchor `range` multi-bloque, widget de diff-marks insertion-only, y `fields.system` fuera del diff de historial.
- 2026-07-09 (sesión 7): se cerró el ítem #1 de Impacto Alto ("Vista unificada cross-section por tag") — nuevo `TaggedItemsService` (`core/tags/`) fanea sobre las 8 entidades taggeables (mismo patrón que `CalendarEventsService`), ruta `/tags/:id` (`TagDetailContainer`) agrupa por kind con el card nativo de cada una, acceso desde `Ctrl+K` con `tag:nombre` → ítem "Ver todo lo de #tag". Descubrimiento a mitad de implementación: el plan original iba a reusar directo los componentes de cada feature, lo cual viola §4.2.10 (feature importando de otra feature) — se resolvió creando twins de solo lectura con inputs primitivos en `shared/entity-cards/` en vez de mover los originales tal cual. Música (`Track`/`Playlist`) quedó afuera por no tener campo `tags` — nueva entrada en `deferred/index.md`. Ver `deferred/index.md` para el detalle completo. La numeración no se recorrió hacia atrás (mismo criterio que sesiones previas) — el grupo Impacto Alto queda temporalmente sin ítems tomables, sólo el árbol de carpetas bloqueado.
- 2026-07-08 (sesión 2): se cerró el ítem #1 de Impacto Alto ("Investigar por qué falla `pushAll`") — ver `deferred/index.md` para el detalle de los 4 bugs encontrados (clasificación de resultado exitoso como error, lookup de ref por nombre corto vs completo, branches de facet nunca creadas, y fetch roto por falta de `git.addRemote()`). La numeración de esta lista no se recorrió hacia atrás para compensar (mismo criterio que las sesiones previas) — tratar los números como referencias históricas y confirmar contra `deferred/index.md` antes de tomar el próximo ítem.
- 2026-07-08 (sesión 3): se cerró el ítem #1 original de Impacto Alto ("Auditar y arreglar las reglas `:global(...)` de `editor.component.css`") — las 9 reglas afectadas se movieron a una hoja global nueva `src/styles/_editor-content.scss`, mismo patrón que `_editor-highlight.scss`/`_book-editor.scss`. Verificado en runtime con el navegador: `.ProseMirror` pasó de `min-height: 0px` a `180px`, `.mc-comment-cloud` matchea. Ver `deferred/index.md` para el detalle. Mismo criterio de numeración: no se recorrió la lista hacia atrás.
- 2026-07-09 (sesión 6): se cerró el ítem #1 de Impacto Alto ("Quick-capture global de nota desde cualquier sección") — atajo `Alt+Shift+N` (no `Ctrl+Shift+N`, reservado por Chrome para incógnito) abre un overlay (`QuickCaptureDialogComponent`) sobre cualquier sección; primera línea del textarea = título, resto = párrafos del cuerpo; la nota cae en la raíz de `/notes` sin tags preseleccionados; confirmación vía toast informativo (`MCB-UI-001`) con acción "Abrir". Ver `deferred/index.md` para el detalle completo de la implementación. Verificado en runtime desde `/books`. La numeración no se recorrió hacia atrás (mismo criterio que sesiones previas).
- 2026-07-09 (sesión 5): reordenado el grupo Impacto Alto a partir del diagnóstico de la sesión 4 — el árbol de carpetas huérfano pasó al final del grupo (marcado bloqueado, sin Tier de esfuerzo asignable) porque requiere una decisión de diseño transversal antes de poder tocar código; quick-capture global y vista cross-section por tag (ambos Tier 4, listos para tomar sin decisión previa pendiente) subieron al #1 y #2. El ítem de `editor.component.css` (cerrado 2026-07-08) se movió a una línea de "Cerrado" al pie del grupo en vez de ocupar el #1 numerado.
- 2026-07-09 (sesión 4): investigado el ítem #2 original de Impacto Alto ("Carpetas / jerarquía real dentro de notas") — resultó ser un diagnóstico desactualizado. El backend y la UI de árbol/carpetas (`FoldersService`, `NotesService.moveToFolder`, `buildFolderTree`, context menu de `folder-actions.ts`) ya están completos y tratan `note` igual que las otras 7 entidades; no faltaba nada de código. El problema real, confirmado en runtime en el navegador: ese panel está oculto en las **8** secciones (`PANE_HIDDEN_PREFIXES` en `workspace-sidebar.container.ts` incluye hoy `/notes`, `/tasks`, `/goals`, `/lists`, `/writings`, `/books`, `/images`, `/files`), no sólo en notas — quedó huérfano a medida que cada sección se rediseñó a vista de tarjetas. También corrige de paso el ítem #9 de Impacto Medio ("Navegación por carpetas / DnD entre carpetas en `/files`"): la frase "la sidebar global ya cubre parcialmente" ya no es cierta para ninguna sección. Ver `deferred/index.md` → "Árbol de carpetas huérfano tras los rediseños de tarjetas" (nueva entrada consolidada, origen paso 6) para el detalle completo; esa entrada reemplaza tanto al ítem #2 de notas como a la mención de `/files`. Es una decisión de diseño transversal (cómo se ve la jerarquía dentro de la metáfora de tarjetas) — no se tocó código de producto esta sesión, sólo se corrigió la documentación. No se recorrió la numeración hacia atrás.
- 2026-07-17 (sesión 9): se cerró el ítem #10 de Impacto Medio ("Sobreescritura completa de defaults del navegador en combos consumidos") — re-audit exhaustivo de todos los `(keydown...)`/`@HostListener('keydown...)` del proyecto. Encontró y corrigió 2 bugs reales: `tts-controls.component.ts` no llamaba `preventDefault()` en su handler de Escape (única excepción entre ~15 handlers similares), y `focus-mode.service.ts` registraba `F11` — fullscreen nativo del navegador, inprevenible desde JS, causaba doble efecto — cambiado a `Alt+Shift+F`. Verificado que ningún combo registrado colisiona con los defaults reservados típicos (`Ctrl+N`/`Ctrl+W`/`Ctrl+T`/`Ctrl+S`/`F1`/`F3`); `Ctrl+P` confirmado como uso intencional y prevenible. Ver `deferred/shortcuts-cross-section.md` para el detalle completo, incluida una desviación arquitectónica menor no bloqueante detectada de paso (dos `@HostListener` document-wide que deberían vivir en `ShortcutsService`, no tocados por estar fuera de alcance de este ítem). No se recorrió la numeración hacia atrás (mismo criterio que sesiones previas).
- 2026-07-10 (sesión 8): agregado el ítem #21 de Impacto Medio ("Responsive mobile — resto de pantallas sin `@media`") — nueva entrada en `deferred/index.md` tras cerrar §19.21 (shell + home + notes). A diferencia de las sesiones previas, esta vez sí se recorrió numeración: el ítem se insertó al final de Impacto Medio (slot #21, antes vacío) y todo el grupo Impacto Bajo se corrió +1 (antes 21-65, ahora 22-66) para no romper el orden por impacto — ningún ítem de Impacto Bajo está referenciado por número en notas de sesiones anteriores, así que el corrimiento es seguro.
- 2026-07-17 (sesión 10): se cerró el ítem #12 de Impacto Medio ("Menú ⋯ con duplicar / exportar a Markdown (books)") — `BooksService.duplicateBook` copia el dir del libro completo (reasigna id de libro y de cada capítulo, remapea `book.order`, reindexa en `SearchIndexService`), siguiendo la misma convención sin sufijo "(copia)" en el título que ya usa `RemindersService`/`onDuplicate`. Exportar a Markdown se resolvió con un converter nuevo y acotado `tiptapToMarkdown` (`core/tiptap/markdown/tiptap-to-markdown.ts`) que sólo cubre los nodos/marcas que este editor realmente produce (heading, listas, citation+atribución, imageRef, bold/italic/strike/code/highlight) — no es un serializer ProseMirror genérico. Tres puntos de entrada: menú ⋯ del meta bar (duplicar libro / exportar libro completo, en `book-open.container`) y un botón nuevo en el header del lector (exportar capítulo activo, en `book-reader.container`). Descarga vía `shared/utils/trigger-download.ts` (extraído como tercera ocurrencia del patrón `createObjectURL`+anchor que ya usaban `ExportZipService` y `files.container.ts`, sin tocar esos dos usos existentes). No hizo falta código de error nuevo: las lecturas de disco ya tiran los `MCB-FS-00x` existentes y el resto es puramente client-side. No se recorrió la numeración hacia atrás (mismo criterio que sesiones previas).
- 2026-07-18 (sesión 11): se cerraron los 3 ítems restantes de `deferred/music.md`. Auditoría previa reveló que "Cover art y duración ID3" (#16, Impacto Medio) y "Waveform pre-renderizado" (#58, Impacto Bajo) ya estaban implementados desde un paso anterior (`id3-reader.ts`, `cover-hash.ts`, `waveform-cache.service.ts`, `track-waveform.component.ts` con click/drag-to-seek) — sólo la doc había quedado desactualizada (regla 24). Trabajo real de esta sesión: (a) miniatura de cover real en el header de cada grupo de álbum de `album-library.container.ts` (antes ícono genérico pese a que `AlbumGroup.coverPath` ya existía), cache de blob URLs por `coverPath` con revocación al desmontar o al salir de pantalla; (b) DnD de tracks a playlist (#51, Impacto Bajo) — se implementó la alternativa que la propia entrada había descartado como "sin caso de uso claro": auto-switch del tab "Playlists" al pasar el drag por encima (delay 350ms tipo auto-expand de carpeta), filas de `playlists-panel.container.ts` como drop target, merge vía `PlaylistsService.addTracks()` nuevo (reusado también por el bulk-add existente de `album-library.container.ts`, que antes duplicaba la misma lógica inline). Ver `deferred/music.md` para el detalle completo.
- 2026-07-18 (sesión 12): dos correcciones sobre la sesión 11. (1) La nota de sesión 11 decía "no se recorrió la numeración hacia atrás" — inexacto: el pre-commit hook (`prettier --write` sobre `*.md` vía lint-staged) renumera automáticamente listas ordenadas con huecos, así que insertar el cierre de "Cover art" entre los ítems #15 y #17 (dejando un hueco en #16) hizo que prettier renumerara 17→16, 18→17, 19→18 al commitear. Se corrigió moviendo esa línea de cierre junto al resto del bloque "Cerrado:" ya agrupado antes del ítem #13 (mismo patrón que los demás cierres de este grupo), restaurando la secuencia limpia 13-18 sin huecos — ningún ítem de Impacto Bajo ni nota anterior referencia los números 17-19 por posición, así que el corrimiento no rompe nada, pero **de acá en más, cualquier cierre que dejaría un hueco en medio de una lista numerada de este doc debe agruparse junto al resto de los "Cerrado:" en vez de insertarse in-place**, o el hook lo va a renumerar solo. (2) El auto-scroll de borde durante drag-and-drop (agregado en la sesión 11 sólo para música) se generalizó a `core/dnd/` (`DragAutoScrollService`, arrancado desde `AppShellContainer`) — un único listener a nivel de documento que cubre cualquier drag-and-drop de la app, no sólo música. Ver `deferred/music.md` para el detalle.
- 2026-08-05 (sesión 13): re-secuenciado todo el grupo Impacto Bajo a pedido explícito del usuario, con dos reglas nuevas: (1) el ítem #15 de Impacto Medio ("CORS proxy propio para push/fetch a GitHub") pasa a tomarse **último de toda la lista**, después de agotar Impacto Bajo, pese a compartir Tier 4 con varios ítems de Bajo — la razón es que requiere infraestructura externa al repo (Cloudflare Worker), no que valga menos; (2) los ítems que decían "no ahora"/"decisión de producto ya tomada" (granularidad por faceta en merge, atajos de tiza, posición libre en `/files`, paginación persistida en books, índice `idx-main`) se reactivan como tomables en vez de quedar marcados como rechazados — la razón original de cada uno queda documentada como restricción de diseño a respetar al implementar, no como veto. Dentro de Impacto Bajo, todo el grupo se renumeró 1-19 por Tier ascendente con ítems de la misma sección agrupados cuando comparten Tier (tiza junto a tiza, `/files` junto a `/files`, palomar junto a palomar, history/versionado junto a history/versionado, books junto a books) — la numeración histórica de este doc (23, 27, 29, 32, 33, 35, 36, 37, 40, 44, 48-50, 54, 56, 59, 63-65) queda obsoleta a partir de esta sesión; no había referencias externas a esos números (verificado con grep sobre `docs/`), así que el corrimiento es seguro.
- 2026-08-05 (sesión 14): tomados los primeros 5 ítems del "Orden de toma" a pedido del usuario ("agarra 5 items pendientes y hazlos"). De esos 5, 2 resultaron ya resueltos (doc desactualizada, misma dinámica que sesiones previas): "Atajos directos a herramientas y colores (tiza)" (`chalk-shortcuts.ts` ya existía y estaba wireado) y "Posición libre real (drag x/y) en `/files`" (`onToggleFreeLayout`/`onItemMove` ya existían). Los otros 3 eran gaps reales, confirmados por grep antes de tocar código, y se implementaron: "Color picker custom para tag" (`Tag.colorHex` + `resolveTagColor()`), "Hilos entre items relacionados (`/files`)" (capa SVG `.thread-layer` con posiciones medidas en vivo), y "Lasso selection para multi-select de pasos (goals)" (`onCanvasPointerDown/Move/Up` + `MultiSelectController.setSelected()`). Los 5 ítems quedaron cerrados — ver las entradas `Cerrado:` correspondientes en Impacto Bajo. Ninguno se verificó visualmente en navegador (sin dev server corriendo en esta sesión) — sólo `tsc --noEmit` limpio + tests unitarios donde aplicaba (tags). No se recorrió numeración hacia atrás; el grupo Impacto Bajo quedó en 1-14 (antes 1-19) descontando los 5 ítems cerrados, siguiendo el mismo criterio de agrupar cierres antes de la lista numerada para no dejar huecos que el hook de prettier reordene solo (ver nota de sesión 12).
